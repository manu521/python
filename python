# atividade 1
# Exibe a mensagem na tela
print("Olá, Mundo!")

# atividade 2
# Exibe a mensagem de boas-vindas
print(f"Olá {nome}, é um prazer te conhecer!")

# atividade 3
# Solicita o nome e o salário do funcionário
nome = input("Nome do Funcionário: ")
salario = float(input("Salário: "))
# Exibe a mensagem formatada
print(f"O funcionário {nome} tem um salário de R${salario:.2f} em Junho.")

# atividade 4
# Solicita dois números inteiros ao usuário
numero1 = int(input("Digite um valor: "))
numero2 = int(input("Digite outro valor: "))
# Calcula a soma
soma = numero1 + numero2
# Exibe o resultado
print(f"A soma entre {numero1} e {numero2} é igual a {soma}.")

# atividade 5
# Solicita as duas notas ao usuário
nota1 = float(input("Nota 1: "))
nota2 = float(input("Nota 2: "))
# Calcula a média
media = (nota1 + nota2) / 2
# Exibe o resultado
print(f"A média entre {nota1} e {nota2} é igual a {media:.1f}")

# atividade 6
# Solicita um número inteiro ao usuário
numero = int(input("Digite um número: "))
# Calcula o antecessor e o sucessor
antecessor = numero - 1
sucessor = numero + 1
# Exibe os resultados
print(f"O antecessor de {numero} é {antecessor}")
print(f"O sucessor de {numero} é {sucessor}")

# atividade 7 
# Solicita um número real ao usuário
numero = float(input("Digite um número: "))
# Calcula o dobro e a terça parte
dobro = numero * 2
terca_parte = numero / 3
# Exibe os resultados
print(f"O dobro de {numero} é {dobro}")
print(f"A terça parte de {numero} é {terca_parte:.5f}")

# atividade 8
# Solicita a distância em metros ao usuário
metros = float(input("Digite uma distância em metros: "))
# Calcula as conversões
km = metros / 1000           # Quilômetros
hm = metros / 100            # Hectômetros
dam = metros / 10            # Decâmetros
dm = metros * 10             # Decímetros
cm = metros * 100            # Centímetros
mm = metros * 1000           # Milímetros
# Exibe os resultados
print(f"A distância de {metros}m corresponde a:")
print(f"{km}Km")
print(f"{hm}Hm")
print(f"{dam}Dam")
print(f"{dm}dm")
print(f"{cm}cm")
print(f"{mm}mm")

# atividade 9 
# Solicita a quantidade de dinheiro em reais ao usuário
reais = float(input("Quanto dinheiro você tem na carteira (em R$): "))
# Conversão para dólares
dolares = reais / 3.45
# Exibe o resultado
print(f"Com R${reais:.2f}, você pode comprar US${dolares:.2f}.")

# atividade 10
# Solicita a largura e a altura da parede
largura = float(input("Digite a largura da parede (em metros): "))
altura = float(input("Digite a altura da parede (em metros): "))
# Calcula a área da parede
area = largura * altura
# Calcula a quantidade de tinta necessária (1 litro cobre 2 m²)
quantidade_tinta = area / 2
# Exibe os resultados
print(f"A área a ser pintada é {area} m².")
print(f"A quantidade de tinta necessária é {quantidade_tinta} litros.")

# atividade 11
# Solicita os coeficientes A, B e C da equação do segundo grau
A = float(input("Digite o valor de A: "))
B = float(input("Digite o valor de B: "))
C = float(input("Digite o valor de C: "))
# Calcula o valor de Delta
delta = (B ** 2) - (4 * A * C)
# Exibe o resultado
print(f"O valor de Delta é {delta}.")

# atividade 12 
# Solicita o preço do produto
preco = float(input("Digite o preço do produto: "))
# Calcula o preço promocional com 5% de desconto
desconto = preco * 0.05
preco_promocional = preco - desconto
# Exibe o resultado
print(f"O preço promocional é R${preco_promocional:.2f}.")

# atividade 13
# Solicita o salário do funcionário
salario = float(input("Digite o salário do funcionário: "))
# Calcula o novo salário com 15% de aumento
aumento = salario * 0.15
novo_salario = salario + aumento
# Exibe o resultado
print(f"O novo salário com 15% de aumento é R${novo_salario:.2f}.")

# atividade 14 
# Solicita a quantidade de Km percorridos e os dias de aluguel
km_percorridos = float(input("Digite a quantidade de Km percorridos: "))
dias_alugados = int(input("Digite a quantidade de dias que o carro foi alugado: "))
# Calcula o preço total
preco_por_dia = 90
preco_por_km = 0.20
custo_total = (dias_alugados * preco_por_dia) + (km_percorridos * preco_por_km)
# Exibe o resultado
print(f"O preço total a pagar é R${custo_total:.2f}.")

# atividade 15
# Solicita o número de dias trabalhados
dias_trabalhados = int(input("Digite o número de dias trabalhados no mês: "))
# Cálculo do salário
horas_por_dia = 8
valor_por_hora = 25
salario = dias_trabalhados * horas_por_dia * valor_por_hora
# Exibe o resultado
print(f"O salário do funcionário é R${salario:.2f}.")

# atividade 16
# Solicita a quantidade de cigarros fumados por dia e quantos anos fumou
cigarros_por_dia = int(input("Quantos cigarros você fuma por dia? "))
anos_fumando = int(input("Quantos anos você fumou? "))
# Calcula a redução de vida em minutos
minutos_perdidos_por_cigarro = 10
total_cigarros = cigarros_por_dia * 365 * anos_fumando
tempo_perdido = total_cigarros * minutos_perdidos_por_cigarro
# Converte para dias
dias_perdidos = tempo_perdido / (60 * 24)
# Exibe o resultado
print(f"Você perderá aproximadamente {dias_perdidos:.2f} dias de vida.")

# atividade 17
velocidade= float(input("informe a velocidade"))
if velocidade > 80 :
    multa = (velocidade - 80) * 5
    print(f"Velocidade acima de 80 km - Multa R$ {multa:.2f}")

# atividade 18
ano_atual = int(input("Digite o ano atual"))
nascimento = int(input("Digite o ano do nascimento"))
idade = ano_atual - nascimento
if idade >= 16 :
    print("Pode votar sua idade é", idade,"anos")
else:
    print("Não pode votar sua idade e ", idade, "anos")

# atividade 19
nome = input("Digite o nome do aluno: ")
nota1 = float(input("Digite a primeira nota: "))
nota2 = float(input("Digite a segunda nota: "))
media = (nota1 + nota2) / 2
print(f"A média do aluno {nome} é: {media:.2f}")
if media >= 7.0:
    print("O aluno teve um bom aproveitamento!")
else:
    print("O aluno não teve um bom aproveitamento.")

# atividade 20
numero = int(input("Digite um número inteiro: "))
if numero % 2 == 0:
    print("O número é PAR.")
else:
    print("O número é ÍMPAR.")

# atividade 21
ano = int(input("Digite um ano: "))
if (ano % 4 == 0 and ano % 100 != 0) or (ano % 400 == 0):
    print(f"O ano {ano} é bissexto.")
else:
    print(f"O ano {ano} não é bissexto.")

# atividade 22
ano_nascimento = int(input("Digite o ano de nascimento: "))
ano_atual = datetime.now().year
idade = ano_atual - ano_nascimento

if idade < 18:
    anos_faltando = 18 - idade
    print(f"Você ainda não tem 18 anos. Faltam {anos_faltando} anos para o seu alistamento.")
elif idade > 18:
    anos_passados = idade - 18
    print(f"Você já tem mais de 18 anos. Já se passaram {anos_passados} anos do seu alistamento.")
else:
    print("Você tem 18 anos. É o ano do seu alistamento.")

# atividade 23
nome = input("Digite o nome do cliente: ")
sexo = input("Digite o sexo (M/F): ").upper()
valor_compras = float(input("Digite o valor das compras: R$"))

if sexo == "M":
    desconto = valor_compras * 0.05
else:
    desconto = valor_compras * 0.13

valor_com_desconto = valor_compras - desconto
print(f"{nome}, o valor com desconto é R${valor_com_desconto:.2f}")

# atividade 24
distancia = float(input("Digite a distância da viagem (em Km): "))

if distancia <= 200:
    preco = distancia * 0.50
else:
    preco = distancia * 0.45

print(f"O preço da passagem é R${preco:.2f}")

# atividade 25
a = float(input("Digite o comprimento do primeiro segmento: "))
b = float(input("Digite o comprimento do segundo segmento: "))
c = float(input("Digite o comprimento do terceiro segmento: "))

if a + b > c and a + c > b and b + c > a:
    print("É possível formar um triângulo.")
else:
    print("Não é possível formar um triângulo.")

# atividade 26
a = int(input("Digite o primeiro número: "))
b = int(input("Digite o segundo número: "))

if a > b:
    print("O primeiro valor é o maior")
elif b > a:
    print("O segundo valor é o maior")
else:
    print("Não existe valor maior, os dois são iguais")

# atividade 27
nota1 = float(input("Digite a primeira nota: "))
nota2 = float(input("Digite a segunda nota: "))
media = (nota1 + nota2) / 2

if media <= 4.9:
    print(f"Média: {media:.1f} - REPROVADO")
elif 5.0 <= media <= 6.9:
    print(f"Média: {media:.1f} - RECUPERAÇÃO")
else:
    print(f"Média: {media:.1f} - APROVADO")

# atividade 28
largura = float(input("Digite a largura do terreno (em metros): "))
comprimento = float(input("Digite o comprimento do terreno (em metros): "))

area = largura * comprimento
print(f"A área do terreno é {area} m².")

if area < 100:
    print("Classificação: TERRENO POPULAR")
elif 100 <= area <= 500:
    print("Classificação: TERRENO MASTER")
else:
    print("Classificação: TERRENO VIP")

# atividade 29
nome = input("Digite o nome do funcionário: ")
salario = float(input("Digite o salário atual: R$"))
anos_trabalho = int(input("Digite quantos anos ele trabalha na empresa: "))

if anos_trabalho <= 3:
    aumento = salario * 0.03
elif 3 < anos_trabalho <= 10:
    aumento = salario * 0.125
else:
    aumento = salario * 0.20

novo_salario = salario + aumento
print(f"{nome}, seu novo salário é R${novo_salario:.2f}")

# atividade 30
a = float(input("Digite o comprimento do primeiro segmento: "))
b = float(input("Digite o comprimento do segundo segmento: "))
c = float(input("Digite o comprimento do terceiro segmento: "))

if a + b > c and a + c > b and b + c > a:
    if a == b == c:
        print("É um triângulo EQUILÁTERO.")
    elif a == b or b == c or a == c:
        print("É um triângulo ISÓSCELES.")
    else:
        print("É um triângulo ESCALENO.")
else:
    print("Não é possível formar um triângulo.")

# atividade 31
import random

opcoes = ['Pedra', 'Papel', 'Tesoura']
computador = random.choice(opcoes)
jogador = input("Escolha: Pedra, Papel ou Tesoura: ").capitalize()

if jogador == computador:
    print(f"Empate! O computador também escolheu {computador}.")
elif (jogador == "Pedra" and computador == "Tesoura") or (jogador == "Papel" and computador == "Pedra") or (jogador == "Tesoura" and computador == "Papel"):
    print(f"Você venceu! O computador escolheu {computador}.")
else:
    print(f"Você perdeu! O computador escolheu {computador}.")

# atividade 32
numero_sorteado = random.randint(1, 5)
tentativas = 0
acertou = False

while tentativas < 4 and not acertou:
    palpite = int(input("Tente adivinhar o número sorteado entre 1 e 5: "))
    tentativas += 1
    if palpite == numero_sorteado:
        acertou = True
        print(f"Você acertou! O número sorteado foi {numero_sorteado}.")
    else:
        print("Tente novamente.")
if not acertou:
    print(f"Você perdeu! O número sorteado foi {numero_sorteado}.")

# atividade 33
valor_casa = float(input("Digite o valor da casa: R$"))
salario = float(input("Digite o salário do comprador: R$"))
anos_pagamento = int(input("Digite em quantos anos vai pagar: "))
prestacao = valor_casa / (anos_pagamento * 12)

if prestacao > 0.30 * salario:
    print("Empréstimo negado! A prestação mensal excede 30% do salário.")
else:
    print(f"Empréstimo aprovado! A prestação mensal será de R${prestacao:.2f}.")

# atividade 34
peso = float(input("Digite o peso (kg): "))
altura = float(input("Digite a altura (m): "))

imc = peso / (altura ** 2)

if imc < 18.5:
    classificacao = "Abaixo do peso"
elif 18.5 <= imc < 25:
    classificacao = "Peso ideal"
elif 25 <= imc < 30:
    classificacao = "Sobrepeso"
elif 30 <= imc < 40:
    classificacao = "Obesidade"
else:
    classificacao = "Obesidade mórbida"

print(f"O IMC é {imc:.2f}. Classificação: {classificacao}")

# atividade 35
tipo_carro = input("Digite o tipo de carro (popular/luxo): ").lower()
dias = int(input("Digite o número de dias de aluguel: "))
km_percorridos = float(input("Digite o número de Km percorridos: "))

if tipo_carro == "popular":
    aluguel = dias * 90
    if km_percorridos <= 100:
        km_preco = km_percorridos * 0.20
    else:
        km_preco = km_percorridos * 0.10
else:
    aluguel = dias * 150
    if km_percorridos <= 200:
        km_preco = km_percorridos * 0.30
    else:
        km_preco = km_percorridos * 0.25

total = aluguel + km_preco
print(f"O preço total a pagar é R${total:.2f}.")

# Exercício 36
horas = float(input("Digite o número de horas de atividade no mês: "))
if horas <= 10:
    pontos = horas * 2
elif horas <= 20:
    pontos = 10 * 2 + (horas - 10) * 5
else:
    pontos = 10 * 2 + 10 * 5 + (horas - 20) * 10
dinheiro = pontos * 0.05
print(f"Você ganhou {pontos} pontos, o que equivale a R${dinheiro:.2f}.")

# Exercício 37
salario = float(input("Digite o salário atual: R$"))
genero = input("Digite o gênero do funcionário (M/F): ").strip().upper()
anos_trabalho = int(input("Digite há quantos anos o funcionário trabalha na empresa: "))
if genero == 'F':
    if anos_trabalho < 15:
        aumento = 0.05
    elif anos_trabalho <= 20:
        aumento = 0.12
    else:
        aumento = 0.23
else:
    if anos_trabalho < 20:
        aumento = 0.03
    elif anos_trabalho <= 30:
        aumento = 0.13
    else:
        aumento = 0.25
novo_salario = salario * (1 + aumento)
print(f"O novo salário do funcionário é R${novo_salario:.2f}.")






